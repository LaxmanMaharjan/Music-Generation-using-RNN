import os

def add_extension():
    ''' Add extension to all midi file in the dataset directory

    Args:
        dataset_path: It is dataset path
    '''
    for file in os.listdir('.')

if __name__ == '__main__':
    
    add_extension(path)
